   <header class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
         <%= link_to logo, root_path, :class=>"logo1" %>

         
            <nav>
            <ul class="nav pull-right">
         <% if signed_in? %>
            <li class="hova"><%= link_to "activity", root_path %></li>
            <li class="hova"><%= link_to "profile", current_user %></li>
            <li class="hova"><%= link_to "invites", invites_path %></li>
           
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-user"></i><b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Settings", current_user %></li>
                <li><%= link_to "Edit Profile", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
         <% else %><li class="pull-left"><%= link_to "Learn More", about_path %></li>
          <ul class="nav pull-right">
          
           
           <li class="dropdown" id="menu1">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#menu1"> Have an account? <b>Log In<b/>
           <b class="caret"></b>
              </a>
             <ul class="dropdown-menu" id="menuout">
               <li>
          <%= form_for(:session, url: sessions_path) do |f| %>
             <%= f.label :email, :class => "bar" %>
             <%= f.text_field :email %>
             <%= f.label :password, :class => "bar" %>
             <%= f.password_field :password %>
            <label class="checkbox">
            <input type="checkbox"> Remember me
            </label>
             <%= f.submit "Sign in", class: "btn btn-large btn-primary" %>
         <% end %>
           </li>
      <% end %>
    </ul>
  </li>
</ul>
       </nav>
      </div>
    </div>
  </header>


